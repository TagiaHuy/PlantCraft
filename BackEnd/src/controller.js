// controller.js
// Controller for handling API logic

module.exports = {
    // Example: get data
    getData: (req, res) => {
        res.json({ message: "API logic handled by controller." });
    }
};